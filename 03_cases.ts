let personname="Ammar";
let per=personname.toUpperCase();
let per1=personname.toLowerCase();
let per2=personname.charAt(0).toUpperCase() + personname.slice(1);
console.log(per) 
console.log(per2) 
console.log(per1) 
export{}